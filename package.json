{
  "name": "n8n-nodes",
  "version": "0.0.0",
  "private": true,
  "description": "Collection of n8n community nodes.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "check": "npm run check --workspaces",
    "release:changed": "node scripts/release-changed-nodes.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
